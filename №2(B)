def doble_power (a, b):
    step = a
    for i in range(0, b):
        step = a * a
        if a > int(a):
            return step
        else:
            return int(step)

a, b = map (float, input().split())
print (doble_power(a,int(b)))

